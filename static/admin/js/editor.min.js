    var E = window.wangEditor;
    var editor = new E('#article-content');
    window.editor = editor;
    window.editor.customConfig.lang = {
        '设置标题': 'title',
        '正文': 'p',
        '链接文字': 'link text',
        '链接': 'link',
        '上传图片': 'upload image',
        '上传': 'upload',
        '创建': 'init'
    };
    editor.customConfig.menus = [
        'head',  // 标题
        'bold',  // 粗体
        'italic',  // 斜体
        'underline',  // 下划线
        'foreColor',  // 文字颜色
        'backColor',  // 背景颜色
        'link',  // 插入链接
        'list',  // 列表
        'justify',  // 对齐方式
        'quote',  // 引用
        'emoticon',  // 表情
        'image',  // 插入图片
        'table',  // 表格
        'code',  // 插入代码
        'undo',  // 撤销
        'redo'  // 重复
    ];
    window.editor.customConfig.pasteFilterStyle = false;
    editor.customConfig.pasteTextHandle = function (content) {
        return content + '<p>来自秒针智能</p>'
    };
    window.editor.customConfig.uploadImgServer = '/upload';
    window.editor.customConfig.debug = location.href.indexOf('wangeditor_debug_mode=1') > 0;
    window.editor.customConfig.uploadImgMaxLength = 5;
    window.editor.create();

    window.editor.customConfig.uploadImgHooks = {
        fail: function (xhr, editor, result) {
            if(result.errno != 0){
                alert(result.msg)
            }
        },

    };